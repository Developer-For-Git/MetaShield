# Gradle Configuration Directory

This directory contains the Gradle build system configurations used to compile and assemble the MetaShield application.

## Contents
* **[wrapper/](wrapper/)** — Contains the Gradle wrapper executable binaries and property definitions to ensure consistent compilation across different environments.
