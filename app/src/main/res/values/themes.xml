<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Compose uses no legacy views, but needs AppCompat for biometric/compat dialogs) -->
    <style name="Theme.MetaShield" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme (windowSplashScreenBrandingImage is API 31+ only, omit here) -->
    <style name="Theme.MetaShield.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_shield_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.MetaShield</item>
    </style>
</resources>
